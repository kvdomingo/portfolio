---

---

<script>
  import posthog from "posthog-js";
  import { PUBLIC_POSTHOG_TOKEN } from "astro:env/client";

  if (import.meta.env.PROD && PUBLIC_POSTHOG_TOKEN) {
    posthog.init(PUBLIC_POSTHOG_TOKEN, {
      api_host: "https://us.posthog.com",
    });
  }
</script>
