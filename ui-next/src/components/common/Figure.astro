---
import { buildCldUrl } from "@/utils/cloudinary";

interface Props {
  publicId: string;
  alt?: string;
  class: string;
}

const { publicId, alt = "", class: className } = Astro.props;
---

<img src={buildCldUrl(publicId)} alt={alt} class={className} />
